'use strict';

// Define the `footer` module
var footer  = angular.module('footer', []);


// Register `footer` component, along with its associated controller and template
footer.
  component('footer', {
    templateUrl: 'components/ui/footer/footer.template.html',
    controller: 
      function FooterController() {
        
      }
  });

